# DevManager

Integrate gitlab issues board + clockify + pomodoro (local) manage time can sync with clockify
`index.html` is main file have html css js, this can deploy to PWA or load as chrome extension use `manifest.json` for chrome extension
`background.js` chrome extension running background, `manifest.webmanifest` pwa manifest, `sw.js` service worker PWA
